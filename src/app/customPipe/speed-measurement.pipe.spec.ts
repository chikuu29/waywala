import { SpeedMeasurementPipe } from './speed-measurement.pipe';

describe('SpeedMeasurementPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeedMeasurementPipe();
    expect(pipe).toBeTruthy();
  });
});
